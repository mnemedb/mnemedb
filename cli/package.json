{
  "name": "mneme-cli",
  "version": "0.3.0",
  "description": "Terminal client for Mneme — the agent-native database on Base. Text-to-SQL, schema-aware chat, live Base ecosystem data, and Mneme Live chain streams (subscribe to onchain events → auto-INSERT into your schema). One key, one prompt.",
  "type": "module",
  "bin": {
    "mneme": "./dist/index.js"
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build":     "tsup src/index.ts --format esm --clean --target node20 --shims",
    "dev":       "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "mneme", "cli", "postgres", "agent", "text-to-sql", "base", "wallet",
    "pgvector", "chat", "claude", "defi", "dexscreener", "tvl", "gas"
  ],
  "license": "MIT",
  "homepage": "https://mnemedb.dev",
  "repository": {
    "type": "git",
    "url":  "https://github.com/mnemedb/mnemedb.git",
    "directory": "cli"
  },
  "author": "mnemedb",
  "dependencies": {
    "mneme-sdk": "^0.6.0",
    "chalk":     "^5.3.0",
    "viem":      "^2.21.0"
  },
  "devDependencies": {
    "tsup":       "^8.0.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
